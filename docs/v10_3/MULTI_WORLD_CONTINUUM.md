# TS-Reasoner v10.3: Multi-World Continuum

v10.3 adds a bounded infrastructure-grid branch-world evaluator. Contradictions
fork isolated worlds, unresolved repairs remain quarantined, and merge
candidates require verifier support.

Run:

```bash
python3 -m ts_reasoner.runtime_os_cli run-continuum --scenario @data/v10_3/infrastructure_grid_scenario.json
```
