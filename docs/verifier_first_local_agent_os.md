# Verifier-First Local Agent OS

v30.0.0 composes the v26-v29 surfaces into one bounded local operating loop.

Release claim:

```text
TS-OS v30 demonstrates a bounded verifier-first local agent operating loop:
natural language requests are translated into typed, risk-classified operations;
unsafe ambiguity abstains; writes and external effects require confirmation;
typed proof objects explain accepted claims;
and every action produces replayable receipts.
```

Run:

```bash
python3 scripts/evaluate_ts_os_v30.py
```

Artifacts:

- `artifacts/ts_os_v30_report.json`
- `artifacts/ts_os_v30_receipt.json`

This is not broad autonomous agency. It is a verifier-first local operating surface with receipts.
